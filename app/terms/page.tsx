import type {FC} from "react"

import TermsClientPage from "./client"

export const metadata = {
	title: `Terms of Service | SprintZero`,
}

const TermsPage: FC = () => {
	return <TermsClientPage />
}

export default TermsPage
