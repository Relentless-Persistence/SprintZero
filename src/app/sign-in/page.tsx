import type {FC} from "react"

import SignInClientPage from "./client"

export const metadata = {
	title: `Sign In | SprintZero`,
}

const SignInPage: FC = () => {
	return <SignInClientPage />
}

export default SignInPage
