import type { FC } from "react"

import CheckoutClientPage from "./client"

export const metadata = {
    title: `Checkout | SprintZero`,
}

const CheckoutPage: FC = () => {
    return <CheckoutClientPage />
}

export default CheckoutPage
