import type { FC } from "react"

import VoiceClientPage from "./client"
export const metadata = {
  title: `Voice | SprintZero`,
}

const VoicePage: FC = () => {
  return <VoiceClientPage />
}

export default VoicePage
