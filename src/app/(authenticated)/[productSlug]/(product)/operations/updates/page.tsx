import type { FC } from "react"

import UpdatesClientPage from "./client"
export const metadata = {
  title: `Updates | SprintZero`,
}

const UpdatesPage: FC = () => {
  return <UpdatesClientPage />
}

export default UpdatesPage