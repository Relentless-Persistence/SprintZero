import type { FC } from "react"

import PracticeClientPage from "./client"
export const metadata = {
    title: `Practice | SprintZero`,
}

const PracticePage: FC = () => {
    return <PracticeClientPage />
}

export default PracticePage
