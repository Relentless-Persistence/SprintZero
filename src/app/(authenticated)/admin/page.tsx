import type {FC} from "react"

import AdminClientPage from "./client"

export const metadata = {
	title: `Admin | SprintZero`,
}

const AdminPage: FC = () => {
	return <AdminClientPage />
}

export default AdminPage
